# ═══════════════════════════════════════════════════════════════════════
# รายชื่อผู้ใช้ JIAN CHA IBP Control Tower
#
#   sudo cp allowed-emails.example.txt /etc/jc-auth/allowed-emails.txt
#   sudo nano /etc/jc-auth/allowed-emails.txt
#
# บรรทัดละหนึ่งอีเมล · ตัวใหญ่ตัวเล็กไม่สำคัญ · ขึ้นต้นด้วย # = คอมเมนต์
# แก้แล้ว "มีผลทันที" ไม่ต้อง restart — คนที่ใช้งานอยู่จะไม่ถูกเตะออก
#
# ลบไฟล์นี้ (และคีย์ allowedEmailsFile ใน config.json) = ให้ทุกคนใน
# โดเมน jianchatea.com ที่ล็อกอินผ่าน Microsoft เข้าได้ทั้งหมด
#
# ระวัง: ถ้า config ชี้มาที่ไฟล์นี้แล้วอ่านไม่ได้ ระบบจะปฏิเสธทุกคนไว้ก่อน
#        (allowlist ที่ปล่อยผ่านเวลาพังไม่ใช่ allowlist)
# ═══════════════════════════════════════════════════════════════════════

# ── ทีม IBP / Demand Planning ──────────────────────────────────────────
# somchai@jianchatea.com
# suda@jianchatea.com

# ── ทีม IT ─────────────────────────────────────────────────────────────
# itadmin@jianchatea.com
